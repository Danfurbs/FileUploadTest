{
  "name": "file-upload-test",
  "version": "1.1.0",
  "description": "Standalone Node.js CLI that stores a local copy of a file.",
  "main": "cli.js",
  "bin": {
    "file-upload": "./cli.js"
  },
  "scripts": {
    "start": "node cli.js",
    "build:standalone": "pkg . --targets node18-linux-x64 --output dist/file-upload"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}
